<h1><%= @task.title %> (<%= @task.details %>)</h1>
<p><%= @task.completed %></p>

<%= link_to "Edit task",edit_task_path(@task) %>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>

<%= link_to "Check All tasks", tasks_path %>
